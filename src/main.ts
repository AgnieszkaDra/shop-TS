function renderApp() {
    const app = document.querySelector('#root');
    if (!app) return;
 }
  
  function initApp() {
    renderApp();
  }
  
  document.addEventListener('DOMContentLoaded', initApp);

